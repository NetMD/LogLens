# Changelog

이 프로젝트의 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 따르며,
[유의적 버전(SemVer)](https://semver.org/lang/ko/)을 준수합니다.

## [1.1.0] - 2026-06-01

여러 로그 파일을 탭으로 동시에 열어 자유롭게 전환하며 보는 뷰어로 확장하고,
구조화 파서가 못 잡는 출력까지 원본 그대로 보는 Raw 보기를 추가했습니다.
모든 변경은 하위 호환이며, 기존 사용자 설정은 자동 마이그레이션됩니다.

### Added
- **다중 파일 탭**: 여러 로그 파일을 탭으로 동시에 열고 자유롭게 전환. 탭 전환 시 스택트레이스/에러 패턴/AI 진단/Export/검색·필터가 모두 활성 파일 기준으로 정확히 동작하며, 탭별 선택·스크롤·검색·레벨 필터 상태를 보존·복원.
- **새 파일 즉시 열기**: 파일이 열린 상태(분석 뷰·도구 탭·실시간 감시 중)에서도 드래그앤드롭/파일 선택으로 새 파일을 바로 새 탭으로 연다. 같은 파일을 다시 드롭하면 이미 열린 탭으로 포커스.
- **Raw 보기**: 구조화 파서가 버리던 비표준·고아 라인(`System.out.println` 출력 등)까지 원본 순서 그대로 보는 보편적 폴백 뷰(mainView "Raw" 탭). 대용량 파일은 가상 스크롤 + 윈도우 읽기로 끊김 없이 표시.
- **실시간 감시 탭 공존**: 정적 파일 탭과 실시간 감시 탭이 한 화면에서 공존. 비활성 실시간 탭은 tail을 일시정지하고 재활성 시 마지막 오프셋부터 catch-up하여 데이터 누락 없이 이어감.

### Changed
- **상태 아키텍처**: 단일 파일 상태를 파일별(`fileId`) 다중 상태 모델로 확장. AI 진단·Export 진행/결과 상태가 파일별로 격리되어 탭을 오가도 섞이지 않음.
- **히스토리 복원**: 과거 분석 복원 시 현재 파일을 덮어쓰지 않고 새 탭으로 연다.
- **메모리 관리**: 동시 탭 수 고정 상한 없이, 메모리 예산 근접 시 오래 비활성인 탭의 데이터를 회수하고 재활성 시 원본에서 무손실 재파싱.

### Security
- Raw 보기용 파일 읽기와 실시간 감시가 동일한 경로 보안 검증(민감 경로 차단, 파일 크기 상한 500MB)을 공유하도록 단일 모듈로 통합.

### Fixed
- 탭 전환 시 레벨 필터 상태가 다른 탭으로 새어 나가거나 복원되지 않던 문제 수정.

## [1.0.1] - 2026-05

### Fixed
- Apple 공증(notarization) 환경 변수 수정 및 macOS portable zip 배포 추가.
- Windows 빌드 경고(미사용 import) 정리, pnpm 버전 고정.

[1.1.0]: https://github.com/loglens/loglens/releases/tag/v1.1.0
[1.0.1]: https://github.com/loglens/loglens/releases/tag/v1.0.1
